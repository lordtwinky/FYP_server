import { WikiEntity, ParamClaimsType } from 'wiki-entity';
export { ParamClaimsType, WikiEntity };
export declare type FindOptions = {
    limit?: number;
    tags?: string[];
    claims?: ParamClaimsType;
    extract?: number;
    redirects?: boolean;
    types?: boolean | string[];
};
export declare function find(name: string, lang: string, options?: FindOptions): Promise<WikiEntity[]>;
